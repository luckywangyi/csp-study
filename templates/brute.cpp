/*
 * CSP 暴力标程模板：用于生成正确的测试数据输出。
 * 不追求效率，只追求正确性。复杂度可以是 O(n^2) 或更高。
 * 编译：g++ -std=c++17 -O2 brute.cpp -o brute
 */
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 暴力解题代码

    return 0;
}
