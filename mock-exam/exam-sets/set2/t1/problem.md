# 折点计数（模拟）

第一行整数 `n`（`3 ≤ n ≤ 100`），第二行 `n` 个互不相同的整数。统计**严格**位于中间的**折点**个数：下标 `i` 满足 `2 ≤ i ≤ n-1`，且 `(a_i-a_{i-1})` 与 `(a_{i+1}-a_i)` 异号（先增后减或先减后增）。

## 样例

输入：

```
5
1 2 3 2 1
```

输出：

```
1
```
