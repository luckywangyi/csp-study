# 解题笔记

## 思路

遍历下标 $i = 1 \ldots n-2$（0 起始则为 `1..n-2`），对每个位置判断是否为**严格峰**或**严格谷**：

- 峰：`a[i] > a[i-1] && a[i] > a[i+1]`
- 谷：`a[i] < a[i-1] && a[i] < a[i+1]`

满足其一则计数加一。

## 知识点

- 简单模拟、局部比较

## 复杂度

- 时间 $O(n)$，空间 $O(n)$ 存数组（或流式读入仅保留三个数可 $O(1)$ 空间）。
