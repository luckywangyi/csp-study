# 解题笔记

## 思路

1. 用 `std::map<int,int>` 或 `unordered_map` 统计每个数出现的次数。
2. 遍历 map：维护当前「最大出现次数」以及「在该次数下最小的数值」。
3. 若当前频次大于已知最大频次，更新答案为该键；若频次相等，用 `min` 保留更小的数。

## 知识点

- 哈希表 / `map` 计数
- 多关键字决策：先比次数，再比数值大小

## 复杂度

- 时间 $O(n \log n)$（`map`）或期望 $O(n)$（`unordered_map`）。
- 空间 $O(n)$。
