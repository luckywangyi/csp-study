# 解题笔记

## 思路

设原矩阵为 $a[n][m]$，结果为 $b[m][n]$。顺时针旋转 $90^\circ$ 的常见对应关系为：

$$b[i][j] = a[n - 1 - j][i]$$

其中 $i \in [0, m)$, $j \in [0, n)$（0 起始下标）。按此双重循环直接输出或存入二维向量即可。

## 知识点

- 矩阵下标变换
- 模拟

## 复杂度

- 时间 $O(nm)$，空间 $O(nm)$（若直接输出可 $O(1)$ 额外空间按序打印）。
